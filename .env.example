# Example .env file for claude-code-mux

# You can use this file to configure your application using environment variables.
# Copy this file to .env and fill in your values.

# -- Router Configuration --
# These variables override the settings in your config.toml file.
# CCM_ROUTER_DEFAULT=default-model-from-env
# CCM_ROUTER_THINK=think-model-from-env
# CCM_ROUTER_BACKGROUND=background-model-from-env
# CCM_ROUTER_WEBSEARCH=websearch-model-from-env

# -- Provider Configuration --
# Use the format CCM_PROVIDER_<PROVIDER_NAME>_API_KEY and CCM_PROVIDER_<PROVIDER_NAME>_BASE_URL
# Replace <PROVIDER_NAME> with the 'name' of the provider in your config.toml, in uppercase, with hyphens replaced by underscores.

# Example for a provider named "openai-main":
# CCM_PROVIDER_OPENAI_MAIN_API_KEY="sk-..."
# CCM_PROVIDER_OPENAI_MAIN_BASE_URL="https://api.openai.com/v1"

# Example for a provider named "anthropic":
# CCM_PROVIDER_ANTHROPIC_API_KEY="sk-ant-..."

# Example for a provider named "openrouter":
# CCM_PROVIDER_OPENROUTER_API_KEY="sk-or-..."
